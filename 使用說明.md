# 題庫練習平台 - 使用說明

## 快速開始

### 1. 安裝依賴

```bash
npm install
```

### 2. 啟動開發伺服器

```bash
npm run dev
```

應用程式將在 `http://localhost:5173` 啟動。

---

## 功能使用指南

### 使用者管理

1. **新增使用者**
   - 在首頁點擊「新增使用者」卡片
   - 輸入使用者名稱
   - 點擊「確認」完成新增

2. **編輯使用者**
   - 點擊使用者卡片上的編輯圖示
   - 修改名稱後按 Enter 或點擊外部區域儲存

3. **刪除使用者**
   - 點擊使用者卡片上的刪除圖示
   - 確認刪除操作

4. **切換使用者**
   - 在首頁點擊「切換使用者」按鈕
   - 選擇要切換的使用者

### 測驗模式

#### 標準模擬模式
1. 選擇題庫（初級或中級）
2. 選擇「標準模擬模式」
3. 開始測驗
4. 填答過程中可自由翻閱上一題修改答案
5. 完成 50 題後點擊「提交問卷」查看結果

#### 即時檢誤模式
1. 選擇題庫（初級或中級）
2. 選擇「即時檢誤模式」
3. 開始練習
4. 選取答案後點擊「確認答案」
5. 系統立即顯示對錯反饋
6. 查看解析後點擊「下一題」繼續

### 題型操作

#### 單選題
- 點擊選項按鈕（圓形）
- 點擊新選項時，舊選項自動取消
- 在即時檢誤模式下，選定後點擊「確認答案」

#### 多選題
- 點擊選項按鈕（方形）可多選
- 再次點擊可取消選擇
- 選取完成後點擊「確認答案」進行判定
- 系統會檢查選取的集合是否與正確答案完全一致

#### 題組題（中級）
- 若題目屬於題組，會顯示「顯示題組題目」按鈕
- 點擊後以彈出視窗顯示該題組的情境內容；若題組含多張圖片，可依序點擊「顯示題組圖片1」「顯示題組圖片2」等查看

### 測試紀錄

1. **查看紀錄列表**
   - 在首頁點擊「測試紀錄」
   - 查看所有歷史測驗紀錄

2. **查看詳情**
   - 點擊紀錄卡片上的「查看詳情」
   - 瀏覽該次測驗的所有題目與答案

3. **篩選檢閱**
   - 在結果頁面使用「全部/正確/錯誤」按鈕
   - 篩選想要檢閱的題目類型
   - 多選題會特別標註「多選」或「漏選」

### 閱讀模式

1. **選擇題庫**
   - 在閱讀模式頁面左側選擇題庫（初級、中級或已收藏）

2. **篩選**
   - **年份**：可選特定年份或「不限制」
   - **類別**：可選特定類別或「不限制」
   - **出題率**：勾選「僅顯示高出題率（2 次以上）」時，只顯示在至少 2 個不同年份出現過的題目（依題目內容相似度 80% 判斷）；選擇出題率時年份強制為不限制
   - 點擊出題率旁的「同步」可重新分析題庫並更新出題率資料；同步成功後會顯示成功訊息

3. **瀏覽題目**
   - 從左側題目列表選擇題目，右側顯示題目詳情、選項與解析
   - 使用出題率篩選時，列表與詳情會顯示該題「出現 N 次」及出現年份

4. **收藏題目**
   - 點擊題目詳情右上角的愛心圖示
   - 收藏的題目會出現在「已收藏」分頁

5. **題目圖片**
   - 若題目有對應圖片，會顯示「顯示圖片」按鈕，點擊後以彈窗查看

### 同步建置（首頁）

- 首頁右上角、主題切換按鈕旁有一個**同步建置**按鈕（重新整理圖示）。
- 點擊後會請伺服器執行一次 `npm run build`，完成後彈窗提示「已同步建置完成，請重新整理頁面以載入最新版本。」，再重新整理頁面即可使用最新建置結果。
- **可用環境**：開發模式（`npm run dev`）、獨立伺服器（`npm run start:standalone`）、以及由執行檔（Windows exe 或 Mac 執行檔）啟動的環境。若在純靜態託管且無後端 API 的環境下，此按鈕會顯示無法連線的提示。

### Windows 可攜式執行檔

- 專案可打包成 **Windows 單一執行檔** `題庫平台.exe`，無需安裝 Node.js 即可執行。
- **建置方式**：在專案目錄執行 `npm run pack:full`，完成後 `dist-portable/` 資料夾內即含 `題庫平台.exe`、`dist/`、`public/`。將整個 `dist-portable` 複製或壓縮分享即可。
- **使用方式**：在該資料夾內雙擊 `題庫平台.exe`，會啟動本機伺服器並自動開啟瀏覽器至 `http://localhost:4173`。關閉程式請從工作管理員結束「題庫平台.exe」。
- **更新內容**：執行檔會從同資料夾讀取 `dist/`、`public/`，因此只要替換該資料夾內的新版 `dist`、`public`，下次執行同一個 exe 即會載入新內容；亦可使用首頁的「同步建置」按鈕在執行檔環境下觸發建置後更新。

### macOS（Macbook）可攜式執行檔

- 專案可打包成 **macOS 執行檔**，供 Intel Mac 與 Apple Silicon（M1/M2/M3）Macbook 使用，無需安裝 Node.js。
- **建置方式**：**請在 Mac 上**於專案目錄執行 `npm run pack:full:mac`（在 Windows 上執行會出現 `spawn UNKNOWN` 錯誤，屬 pkg 交叉編譯限制）。完成後 `dist-portable/` 內會產生 `題庫平台-macos-x64`（Intel）、`題庫平台-macos-arm64`（Apple Silicon），以及 `dist/`、`public/`。將整個 `dist-portable` 複製到 Mac 或壓縮分享即可。
- **使用方式**：在 Mac 上開啟終端機，進入該資料夾後：
  - **Apple Silicon（M1/M2/M3）**：先執行 `chmod +x 題庫平台-macos-arm64` 賦予執行權限，再執行 `./題庫平台-macos-arm64`
  - **Intel**：先執行 `chmod +x 題庫平台-macos-x64`，再執行 `./題庫平台-macos-x64`
  程式會啟動本機伺服器並自動開啟瀏覽器至 `http://localhost:4173`。於終端機按 `Ctrl+C` 可停止伺服器。
- **更新內容**：與 Windows 版相同，執行檔從同資料夾讀取 `dist/`、`public/`，替換該資料夾內的新版即可載入新內容。

### 題庫管理

- 在首頁「題庫管理」區塊可進行：
  - **匯入題庫**：上傳 JSON 題庫檔，依題目 id 前贅詞分組，有重複時可選擇僅匯入不重複或覆蓋並匯入
  - **匯入圖片**：上傳題目對應圖片（檔名須與題目 ID 一致），會寫入 `public/question-images`
  - **圖片同步**：點擊後彈窗顯示上次同步時間與目前圖片數量，確認後執行 manifest 生成，更新圖片清單
  - **匯出題庫**：可依「匯出年份」「匯出類別」篩選後匯出為 JSON

題目格式、題組格式、初階 ID 區分科目等詳見根目錄 **題庫管理說明.md**。腳本指令與圖片 API 見 **題庫腳本說明.md**、**題目圖片管理說明.md**。

---

## 技術支援

### 常見問題

**Q: 題庫無法載入？**  
A: 請確認題目格式符合 JSON 規範，並檢查 `questionBank` 欄位是否為 `primary` 或 `intermediate`。可執行 `npm run validate-questions` 檢查格式。

**Q: 多選題判斷不正確？**  
A: 請確認 `correctAnswers` 陣列包含所有正確答案的 ID，且順序不影響判斷；選項 ID 建議使用 `opt_a`、`opt_b` 等。

**Q: 資料遺失？**  
A: 系統使用 localStorage 儲存使用者、收藏、測驗紀錄與出題率資料，清除瀏覽器資料會導致資料遺失，請勿清除站點資料。

**Q: 出題率篩選沒有題目？**  
A: 請先在閱讀模式點擊出題率旁的「同步」按鈕，執行一次同步後才會產生出題率資料；僅會顯示在至少 2 個不同年份出現過的題目。

**Q: 題目有圖片但沒有「顯示圖片」按鈕？**  
A: 請確認圖片檔名與題目 ID 完全一致，並執行「題庫管理」的「圖片同步」或本機執行 `npm run generate-image-manifest` 更新 manifest。

**Q: 圖片同步按鈕點下去沒反應或失敗？**  
A: 圖片同步需透過支援 API 的環境（`npm run dev` 或 `npm run start`）才會生效；若為純靜態部署，請在本機執行 `npm run generate-image-manifest` 後再部署。

**Q: 首頁「同步建置」按鈕點下去出現 404 或無法連線？**  
A: 同步建置需在「有提供 `/api/trigger-build` 的環境」下使用。開發時請使用 `npm run dev`（已支援此 API）；若使用執行檔或 `npm run start:standalone` 也會支援。純靜態部署（無後端）時此功能無法使用，請在本機建置後再部署。

### 新增功能時可能遇到的問題

**Q: 新增頁面後路由沒生效？**  
A: 在 `src/App.tsx` 的 `<Routes>` 內新增對應的 `<Route path="..." element={...} />`，並確認有從適當入口（例如首頁）導向該路徑。

**Q: 新增的狀態在重新整理後消失？**  
A: 若需持久化，可將狀態存到 localStorage 或使用現有 store（如 `userStore`、`questionStore`）；可參考 `src/store` 內現有寫法。

**Q: 題庫資料要從哪裡讀？**  
A: 題目由 `src/data/questionBanks.ts` 載入，實際題目檔在 `src/data/banks/*.json`；透過 `useQuestionStore()` 的 `getQuestions(bank)` 取得題目列表。

**Q: 想新增 API（例如後端呼叫）？**  
A: 開發時可透過 Vite 的 plugin 在 `scripts/` 下掛 API（例如 `scripts/vite-plugin-save-question-images.js` 已掛載圖片 API 與 `/api/trigger-build`）；生產環境則由 `scripts/server.mjs` 與 `scripts/standalone-server.cjs` 提供靜態檔與 API，可參考現有 `/api/check-question-images`、`/api/get-image-manifest`、`/api/trigger-build` 等寫法。

**Q: 題目類型或選項結構要擴充？**  
A: 先修改 `src/types/index.ts` 的題目相關型別，再檢查 `src/utils/importExport.ts`、`validateQuestions` 及各頁面使用處，避免遺漏驗證或顯示邏輯。

**Q: 樣式與現有主題不一致？**  
A: 請使用 `src/styles/globals.css` 中定義的 CSS 變數（如 `var(--color-primary)`、`var(--spacing-md)`），並避免硬編碼色碼，以支援淺色/深色主題。

### 開發模式

系統在開發模式下會自動載入題庫（來自 `src/data/banks/*.json`），方便測試功能。

---

## 注意事項

- 中級題庫包含複選題，請確保題目格式中 `type` 為 `"multiple"` 且 `correctAnswers` 正確。
- 所有使用者、收藏、測驗紀錄與出題率資料均儲存在瀏覽器本地，不會上傳到伺服器。
- 建議定期備份重要資料（可透過「匯出題庫」備份題目）。

---

## 功能檔案說明

以下簡要說明各功能對應的負責檔案，方便維護與擴充。

| 功能 | 負責檔案 | 說明 |
|------|----------|------|
| 路由與應用入口 | `src/App.tsx` | 定義所有路由、主題初始化、題庫與圖片 manifest 預載 |
| 使用者選擇與登入 | `src/pages/UserSelection.tsx` | 使用者選擇/新增頁面 |
| 使用者狀態 | `src/store/userStore.ts` | 當前使用者、使用者列表的 Zustand store |
| 首頁 | `src/pages/Home.tsx` | 題庫選擇、測驗模式入口、題庫管理、測試紀錄入口、同步建置按鈕 |
| 題庫選擇區塊 | `src/components/QuestionBankSelector.tsx` | 初級/中級題庫選擇 UI |
| 測驗設定 | `src/pages/TestSetup.tsx` | 選擇測驗模式（標準模擬/即時檢誤） |
| 測驗進行 | `src/pages/Test.tsx` | 答題流程、翻題、即時檢誤判定、題組/圖片按鈕 |
| 測驗結果 | `src/pages/Result.tsx` | 成績摘要、全部/正確/錯誤篩選、題目檢閱 |
| 測驗狀態 | `src/store/testStore.ts` | 當前測驗進度、答案、紀錄的 Zustand store |
| 選項按鈕 | `src/components/OptionButton.tsx` | 單選/多選選項 UI |
| 題目資料與題庫列表 | `src/store/questionStore.ts` | 題目列表、年份/類別、匯入後的題庫狀態 |
| 題庫載入 | `src/data/questionBanks.ts` | 從 `src/data/banks/*.json` 載入題庫 |
| 題型與資料型別 | `src/types/index.ts` | Question、QuestionBank、Option 等型別定義 |
| 題目工具 | `src/utils/questionUtils.ts` | 題目相關共用函式 |
| 題組判斷與內容 | `src/utils/questionGroupUtils.ts` | 題組 key、是否題組題等邏輯 |
| 題組內容彈窗 | `src/components/QuestionGroupContentModal.tsx` | 顯示題組情境與題組圖片 |
| 測試紀錄列表 | `src/pages/Records.tsx` | 歷史測驗紀錄列表與詳情入口 |
| 閱讀模式 | `src/pages/Reading.tsx` | 題庫/年份/類別/出題率篩選、題目列表、詳情、收藏、出題率同步彈窗 |
| 收藏狀態 | `src/store/favoriteStore.ts` | 使用者收藏題目的 Zustand store |
| 出題率分析與儲存 | `src/utils/questionFrequency.ts` | 依相似度分析跨年份重複、localStorage 儲存、取得高出題率題目與次數/年份 |
| 題庫管理（匯入/匯出/圖片） | `src/components/ImportExportPanel.tsx` | 匯入題庫 JSON、匯入圖片、圖片同步、匯出題庫與篩選 |
| 匯入匯出邏輯 | `src/utils/importExport.ts` | JSON 驗證、重複判斷、匯入/匯出題目 |
| 題目圖片清單與查詢 | `src/utils/questionImages.ts` | 讀取 manifest、預載、同步查詢題目是否有圖片 |
| 題目圖片彈窗 | `src/components/QuestionImageModal.tsx` | 顯示單一題目圖片 |
| 主題 | `src/store/themeStore.ts` | 淺色/深色主題狀態 |
| 全域樣式 | `src/styles/globals.css` | CSS 變數與全域樣式 |
| 圖片 manifest 生成 | `scripts/generate-question-image-manifest.js` | 掃描 `public/question-images` 產生 manifest.json |
| 圖片 API（檢查/儲存/manifest） | `scripts/question-images-api.js` | 提供檢查現有圖片、儲存上傳圖片、讀取/生成 manifest 的 handler |
| 開發環境圖片 API 與同步建置 | `scripts/vite-plugin-save-question-images.js` | Vite 開發時掛載圖片相關 API 及 POST `/api/trigger-build` |
| 生產環境靜態與 API | `scripts/server.mjs` | 靜態檔與題目圖片 API、同步建置 API |
| 可攜式執行檔伺服器 | `scripts/standalone-server.cjs` | 執行檔與 `npm run start:standalone` 使用的靜態檔與 API（含 trigger-build） |

題庫格式與指令詳見：**題庫管理說明.md**、**題庫腳本說明.md**、**題目圖片管理說明.md**。
