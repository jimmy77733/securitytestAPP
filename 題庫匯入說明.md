# 題庫匯入說明

## 題目格式

系統支援從JSON格式匯入題目。請將題目整理成以下格式：

```json
[
  {
    "id": "q_1",
    "questionBank": "primary",
    "type": "single",
    "question": "題目內容",
    "options": [
      { "id": "opt_1", "text": "選項1" },
      { "id": "opt_2", "text": "選項2" },
      { "id": "opt_3", "text": "選項3" },
      { "id": "opt_4", "text": "選項4" }
    ],
    "correctAnswers": ["opt_1"],
    "explanation": "解析說明（可選）",
    "year": "2024（可選）",
    "category": "分類名稱（可選）"
  },
  {
    "id": "q_2",
    "questionBank": "intermediate",
    "type": "multiple",
    "question": "多選題題目內容",
    "options": [
      { "id": "opt_1", "text": "選項1" },
      { "id": "opt_2", "text": "選項2" },
      { "id": "opt_3", "text": "選項3" },
      { "id": "opt_4", "text": "選項4" }
    ],
    "correctAnswers": ["opt_1", "opt_2", "opt_3"],
    "explanation": "多選題解析說明"
  }
]
```

## 欄位說明

- **id**: 題目唯一識別碼（建議格式：`q_primary_108-2_1` 或 `q_primary_114-1_m_5`、`q_primary_113-2_s_1`，見下方初階 ID 區分科目）
- **questionBank**: 題庫類型，`"primary"` 或 `"intermediate"`
- **type**: 題型，`"single"`（單選）或 `"multiple"`（多選）
- **question**: 題目文字
- **options**: 選項陣列，每個選項包含 `id` 和 `text`
- **correctAnswers**: 正確答案的ID陣列（單選題只有一個，多選題可有多個）
- **explanation**: 解析說明（選填）
- **year**: 年份（選填）
- **category**: 分類（選填）
- **questionGroupId**: 題組題時填寫（選填），與同題庫／科目／年份的題組內容對應，作答時會顯示「顯示題組題目」按鈕

### 初階題目 ID 區分科目

為避免題目圖片與不同科目重複，初階題目請依 `category` 在 id 尾號前加入前贅詞：

- **資訊安全管理概論**：id 格式為 `q_primary_年份-期別_m_題序`，例：`q_primary_114-1_m_5`
- **資訊安全技術概論**：id 格式為 `q_primary_年份-期別_s_題序`，例：`q_primary_113-2_s_1`

## 匯入方式

### 方法1：使用開發者工具

1. 開啟瀏覽器開發者工具（F12）
2. 在Console中執行：

```javascript
// 假設題目資料在變數 questions 中
import { loadQuestionBankFromJSON } from './src/utils/questionLoader';
loadQuestionBankFromJSON('primary', questions);
```

### 方法2：修改初始化腳本

在 `src/utils/questionLoader.ts` 中直接添加題目資料，然後在 `initApp` 中載入。

### 方法3：建立匯入頁面（建議）

可以建立一個管理頁面，讓使用者上傳JSON檔案並自動匯入。

## 題組題格式（中級）

題組題為多題共用同一情境／圖片。作法如下：

1. 題目加上 `questionGroupId`（例如 `"1"` 表示題組 1），且同一題庫、同一 `category`、同一 `year` 才會對應到同一題組內容。
2. 同一題庫 JSON 可為物件格式：`{ "questions": [...], "questionGroups": [...] }`。
3. **questionGroups** 每筆欄位：
   - **groupKey**：唯一鍵，建議格式 `題庫_科目_年份_題組編號`，例：`intermediate_資訊安全防護實務_108_1`
   - **contentText**（選填）：題組情境文字
   - **imageIds**：題組圖片 ID 陣列，順序即顯示順序；圖片檔名為 `{imageId}.png`，置於 `public/question-images/`
4. 作答時會顯示「顯示題組題目」按鈕，點擊後以彈出視窗顯示題組內容與「顯示題組圖片1」「顯示題組圖片2」等按鈕。

範例請參考 `src/data/questionGroupFormatSample.json`。

## 注意事項

- 中級題庫包含複選題，請確保 `type` 欄位正確設定為 `"multiple"`
- 多選題的 `correctAnswers` 必須包含所有正確答案的ID
- 選項ID必須唯一，建議使用 `opt_a`, `opt_b` 等格式
- 題目ID建議使用有意義的命名，方便後續管理；初階兩科目請依上述規則加上 `_m_` 或 `_s_` 前贅詞

