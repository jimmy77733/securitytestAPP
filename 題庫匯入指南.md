# 題庫匯入完整指南

## 當前狀況

由於PDF文件格式複雜，自動解析功能無法完美提取所有題目。目前系統已準備好接收手動整理的題目數據。

## 快速開始

### 步驟1：準備題目數據

題目數據需要以JSON格式儲存在以下文件：
- `data/primary-questions.json` - 初級題庫
- `data/intermediate-questions.json` - 中級題庫

### 步驟2：驗證題目格式

執行驗證命令：

```bash
npm run validate-questions
```

這會檢查所有題目的格式是否正確。

### 步驟3：生成載入代碼

如果驗證通過，執行：

```bash
npm run load-questions
```

這會生成 `src/data/questionBanks.ts` 文件，包含所有題目數據。

### 步驟4：啟動應用

```bash
npm run dev
```

系統會自動載入題庫。

## 題目格式範例

### 單選題（初級題庫）

```json
{
  "id": "q_primary_108-2_1",
  "questionBank": "primary",
  "type": "single",
  "question": "關於資料在傳輸層使用TLS協定進行資料的保護，下列敘述何者「不」正確？",
  "options": [
    { "id": "opt_a", "text": "透過加密而能確保資訊的私密性" },
    { "id": "opt_b", "text": "藉由加密來確保訊息的完整性" },
    { "id": "opt_c", "text": "經由數位授權提供資料的真實性" },
    { "id": "opt_d", "text": "經由公開金鑰進行加解密的過程" }
  ],
  "correctAnswers": ["opt_c"],
  "explanation": "TLS協定主要提供機密性和完整性保護，真實性通常由數位憑證提供，而非數位授權。",
  "year": "108-2",
  "category": "資訊安全技術概論"
}
```

### 多選題（中級題庫）

```json
{
  "id": "q_intermediate_109_1",
  "questionBank": "intermediate",
  "type": "multiple",
  "question": "某公司正準備規劃其識別及存取管理（Identity and Access Management）機制，下列何者是可選擇的存取控制類型？（複選）",
  "options": [
    { "id": "opt_a", "text": "強制存取控制（Mandatory Access Control, MAC）" },
    { "id": "opt_b", "text": "識別存取控制（Identity-Based Access Control, IBAC）" },
    { "id": "opt_c", "text": "規則基礎存取控制（Rule-Based Access Control, RuBAC）" },
    { "id": "opt_d", "text": "自主存取控制（Discretionary Access Control, DAC）" }
  ],
  "correctAnswers": ["opt_a", "opt_c", "opt_d"],
  "year": "109",
  "category": "資訊安全規劃實務"
}
```

## 欄位說明

| 欄位 | 類型 | 必填 | 說明 |
|------|------|------|------|
| id | string | 是 | 題目唯一識別碼 |
| questionBank | string | 是 | 'primary' 或 'intermediate' |
| type | string | 是 | 'single' 或 'multiple' |
| question | string | 是 | 題目文字 |
| options | array | 是 | 選項陣列，至少2個 |
| correctAnswers | array | 是 | 正確答案ID陣列 |
| explanation | string | 否 | 解析說明 |
| year | string | 否 | 年份 |
| category | string | 否 | 分類 |

## 注意事項

1. **ID唯一性**：每個題目的ID必須唯一
2. **選項ID格式**：必須是 `opt_a`, `opt_b`, `opt_c`, `opt_d`
3. **正確答案**：必須是選項ID的陣列，且所有ID都必須在options中存在
4. **多選題**：中級題庫包含複選題，`type` 必須設為 `"multiple"`，且 `correctAnswers` 應包含多個ID
5. **年份格式**：
   - 初級題庫：`108-2`（年份-期別）
   - 中級題庫：`108`（年份）

## 批量處理建議

1. **分批處理**：一次處理一個PDF文件對應的題目
2. **先提取題目**：先提取所有題目和選項（不包含答案）
3. **後添加答案**：統一添加正確答案
4. **驗證格式**：每批處理後執行驗證

## 工具命令

```bash
# 驗證題目格式
npm run validate-questions

# 生成載入代碼
npm run load-questions

# 解析PDF（實驗性，可能無法完美提取）
npm run parse-pdf
```

## 題目統計目標

- **初級題庫**：26個PDF × 50題 ≈ 1300題
- **中級題庫**：20個PDF × 40題 ≈ 800題
- **總計**：約2100題

## 常見問題

**Q: 如何知道題目格式是否正確？**
A: 執行 `npm run validate-questions` 會顯示所有錯誤和警告。

**Q: 題目添加後如何載入到系統？**
A: 執行 `npm run load-questions` 生成載入代碼，系統會在啟動時自動載入。

**Q: 可以分批添加題目嗎？**
A: 可以，每次添加後執行驗證和載入即可。

**Q: 多選題的答案如何設定？**
A: `correctAnswers` 設為包含多個選項ID的陣列，例如：`["opt_a", "opt_b", "opt_c"]`

## 下一步

1. 開始手動整理題目到JSON文件
2. 使用驗證工具檢查格式
3. 生成載入代碼
4. 在系統中測試題目顯示和功能

