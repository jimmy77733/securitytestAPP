# 題庫管理說明

本說明整合題庫匯入、格式、前端匯入流程與題組題格式，供題庫管理與匯入時參考。

---

## 題目格式概覽

題目以 JSON 格式儲存。可從兩處著手：

- **後端/腳本**：將題目放在 `data/primary-questions.json`、`data/intermediate-questions.json` 或 `data/banks/*.json`，經由驗證與載入腳本寫入 `src/data/banks/*.json`。
- **前端介面**：在首頁「題庫管理」上傳 JSON 檔，系統依題目 id 前贅詞分組並寫入題庫。

---

## 題目格式範例

### 單選題（初級題庫）

```json
{
  "id": "q_primary_skill_108-2_s_1",
  "questionBank": "primary",
  "type": "single",
  "question": "關於資料在傳輸層使用TLS協定進行資料的保護，下列敘述何者「不」正確？",
  "options": [
    { "id": "opt_a", "text": "透過加密而能確保資訊的私密性" },
    { "id": "opt_b", "text": "藉由加密來確保訊息的完整性" },
    { "id": "opt_c", "text": "經由數位授權提供資料的真實性" },
    { "id": "opt_d", "text": "經由公開金鑰進行加解密的過程" }
  ],
  "correctAnswers": ["opt_c"],
  "explanation": "TLS協定主要提供機密性和完整性保護，真實性通常由數位憑證提供，而非數位授權。",
  "year": "108-2",
  "category": "資訊安全技術概論"
}
```

### 多選題（中級題庫）

```json
{
  "id": "q_intermediate_109-1_o_1",
  "questionBank": "intermediate",
  "type": "multiple",
  "question": "某公司正準備規劃其識別及存取管理（Identity and Access Management）機制，下列何者是可選擇的存取控制類型？（複選）",
  "options": [
    { "id": "opt_a", "text": "強制存取控制（Mandatory Access Control, MAC）" },
    { "id": "opt_b", "text": "識別存取控制（Identity-Based Access Control, IBAC）" },
    { "id": "opt_c", "text": "規則基礎存取控制（Rule-Based Access Control, RuBAC）" },
    { "id": "opt_d", "text": "自主存取控制（Discretionary Access Control, DAC）" }
  ],
  "correctAnswers": ["opt_a", "opt_c", "opt_d"],
  "year": "109",
  "category": "資訊安全規劃實務"
}
```

### 初階題目 ID 與科目區分

為避免題目圖片錯用，初階題目請依 `category` 在 id 尾號前加前贅詞：

- **資訊安全管理概論**：`q_primary_114-1_m_5`（尾號前加 `_m_`）
- **資訊安全技術概論**：`q_primary_113-2_s_1`（尾號前加 `_s_`）

---

## 欄位說明

| 欄位 | 類型 | 必填 | 說明 |
|------|------|------|------|
| id | string | 是 | 題目唯一識別碼，建議格式見上方初階 ID 區分 |
| questionBank | string | 是 | `primary` 或 `intermediate` |
| type | string | 是 | `single` 或 `multiple` |
| question | string | 是 | 題目文字 |
| options | array | 是 | 選項陣列，每項含 `id`、`text`，至少 2 個 |
| correctAnswers | array | 是 | 正確答案 ID 陣列（單選一個，多選多個） |
| explanation | string | 否 | 解析說明 |
| year | string | 否 | 年份（初級例：108-2，中級例：108） |
| category | string | 否 | 分類 |
| questionGroupId | string | 否 | 題組題時填寫，與 questionGroups 的 groupKey 對應（需同題庫、科目、年份） |

選項 ID 建議使用 `opt_a`、`opt_b`、`opt_c`、`opt_d`。

---

## 題組題格式（中級）

題庫 JSON 可為物件：`{ "questions": [...], "questionGroups": [...] }`。

1. 題目加上 `questionGroupId`（例如 `"1"` 表示題組 1），且同一題庫、同一 `category`、同一 `year` 才會對應到同一題組內容。
2. **questionGroups** 每筆欄位：
   - **groupKey**：唯一鍵，建議格式 `題庫_科目_年份_題組編號`，例：`intermediate_資訊安全防護實務_108_1`
   - **contentText**（選填）：題組情境文字
   - **imageIds**：題組圖片 ID 陣列，順序即顯示順序；圖片檔名為 `{imageId}.png`，置於 `public/question-images/`
3. 作答時會顯示「顯示題組題目」按鈕，點擊後以彈出視窗顯示題組內容與「顯示題組圖片1」「顯示題組圖片2」等。

範例請參考 `src/data/questionGroupFormatSample.json`。

---

## 前贅詞與檔名

- 題目 id 的**前贅詞**＝去掉結尾「底線＋數字」後的字串。  
  例：`q_primary_108-2_1` → 前贅詞 `q_primary_108-2`
- 每個前贅詞對應一個檔案：`src/data/banks/<前贅詞>.json`
- 應用啟動時由 `src/data/questionBanks.ts` 讀取所有 `banks/*.json` 並合併載入

---

## 使用腳本驗證與載入（可選）

若題目放在 `data/` 目錄：

### 驗證題目格式

```bash
npm run validate-questions
```

會檢查 `data/primary-questions.json`、`data/intermediate-questions.json` 等格式是否正確。

### 依前贅詞分檔並寫入

```bash
npm run load-questions
```

會讀取上述 JSON，依題目 id 前贅詞分組，為每個前贅詞產生 `src/data/banks/<前贅詞>.json`，不會覆寫 `src/data/questionBanks.ts` 的載入邏輯。

---

## 前端匯入流程（題庫管理）

從首頁「題庫管理」匯入 JSON 時：

1. **判斷前贅詞**：依題目 id 取得前贅詞並分組
2. **無對應檔案**：該前贅詞在目前題庫中沒有題目 → 直接新增（整批加入）
3. **有對應檔案**：該前贅詞已有題目 → 檢查是否有重複 id  
   - 若有重複：顯示差異與覆蓋確認視窗，可選擇「僅匯入不重複的」或「覆蓋並匯入」
4. **排序**：新加入的題目會接在現有題目之後，依序排列

---

## 注意事項

1. **ID 唯一性**：每個題目的 id 必須唯一
2. **正確答案**：必須是選項 id 的陣列，且所有 id 都必須在 options 中存在
3. **多選題**：中級題庫複選題 `type` 須為 `"multiple"`，`correctAnswers` 可包含多個 id
4. **初階兩科目 ID**：`category` 為「資訊安全管理概論」者 id 尾號前加 `_m_`，「資訊安全技術概論」者加 `_s_`，避免題目圖片與他科重複

---

## 常見問題

**Q: 如何知道題目格式是否正確？**  
A: 執行 `npm run validate-questions` 會顯示錯誤與警告；前端匯入時也會進行基本驗證。

**Q: 題目添加後如何載入到系統？**  
A: 若用腳本：執行 `npm run load-questions` 會寫入 `src/data/banks/*.json`，系統啟動時自動載入。若用前端匯入：上傳 JSON 後即寫入題庫並即時生效。

**Q: 可以分批添加題目嗎？**  
A: 可以，每次添加後執行驗證與載入（或多次使用前端匯入）即可。

**Q: 多選題的答案如何設定？**  
A: `correctAnswers` 設為包含多個選項 id 的陣列，例如 `["opt_a", "opt_b", "opt_c"]`。

更多腳本指令（如題目 ID 前贅詞批次更新、題組重複 key 合併）請見 **題庫腳本說明.md**。
